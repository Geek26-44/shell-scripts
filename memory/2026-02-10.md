# 2026-02-10

## Обновление OpenClaw
- Была версия 2026.2.6-3, обновились до **2026.2.9**
- Проблема: npm кэш и `/usr/local/` были под root — EACCES ошибки
- Решение: `sudo chown -R $(whoami) ~/.npm /usr/local/lib/node_modules /usr/local/bin`
- **Урок:** после установки через sudo права ломаются, нужно чинить ownership

## Оптимизация Mac mini
- Диск: 17GB/228GB (9%) — отлично
- Swap: 0 — RAM хватает
- Убили: Safari (~500MB RAM), AppleSpell (~200MB), видео-обои (56 мин CPU)
- Обои заменены на чёрные (статичные) — WindowServer упал с 27% до 13% CPU
- Почищены кэши, DNS сброшен
- `avconferenced` + `VTEncoderXPCService` — держит Screen Sharing, убивать бесполезно (respawn)
- 466 launch agents — много, но без ревизии не трогаем

## Задача: постоянная "память"
- Дима хочет чтобы я всегда был с контекстом между сессиями
- Настроена система памяти (MEMORY.md + daily files + memory_search)

## Fallback модели — критично!
- Вчера (~7 PM) Claude упал по лимиту токенов — я перестал работать
- Дима хочет чтобы я НИКОГДА не терял "мозги"
- Gemini подключен как CLI скилл, но НЕ как провайдер OpenClaw
- **Нужен Gemini API ключ** чтобы добавить как провайдер (openclaw onboard или aistudio.google.com/apikey)
- Ollama fallback настроен в конфиге

## Мульти-агентная архитектура (из вчерашнего чата)
- Подробный план в memory/architecture.md
- Схема: Дима → Ollama Router (Qwen 14B) → простое/сложное/код
- Простое → Ollama сам, Сложное → Opus (с разрешения), Код → DeepSeek (локально)
- DeepSeek ТОЛЬКО для проверки кода, не для кодинга
- Все локальные модели через Ollama — ничего не утекает наружу

## Ollama + модели
- Ollama запущен (brew services)
- **Qwen 2.5 14B** — скачан, работает (тест через API: OK)
- **Gemma 3 12B** — скачана, работает
- CLI ollama run зависает на 14B (RAM впритык), но API работает
- Конфиг fallback: Claude → Qwen 14B → Gemma 12B
- **OLLAMA_API_KEY** добавлен в ~/.zprofile, но Gateway нужно перезапустить с этой переменной
- Диск после моделей: ~17GB моделей из 171GB свободно

## Оптимизация Mac mini
- Обои → чёрные (WindowServer с 27% до 13% CPU)
- Safari убит (~500MB RAM)
- avconferenced/VTEncoder — держит Screen Sharing, убивать бесполезно
- Кэши почищены, DNS сброшен

## TODO (следующая сессия)
- [ ] Перезапустить Gateway с OLLAMA_API_KEY для Ollama discovery
- [ ] Получить Gemini API ключ → добавить как провайдер (средний fallback)
- [ ] Этап 2: роутер-плагин (классификатор через Qwen)
- [ ] Проверить что Qwen 14B стабильно работает при нагрузке (RAM)
