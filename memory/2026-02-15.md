# 2026-02-15

## Контекст сессии

- Дима попросил начать Фазу 4
- Восстановили полную историю из транскрипта предыдущей сессии (fbad46b0)
- Подтвердили что Фазы 1-3 + security hardening завершены

## Статус на начало дня

### Git (6 коммитов на master):
```
11ce14e  security: hardening — confidence, PII, failsafe, safe mode
693f1dd  Phase 3: skill + audit logging + log viewer
41c6056  perf: postprocessor 4x faster (7B)
c973f02  Phase 2: router integrated with OpenClaw
b606c35  chore: .gitignore
96e9275  Phase 1: workspace baseline
```

### Что готово:
- Qwen Router (classify, PII filter, translate, self-check) — ✅
- Постпроцессор оптимизирован (7B для переводов, 4x быстрее) — ✅
- OpenClaw скилл `qwen-router` зарегистрирован — ✅
- Аудит логирование (что ушло наружу) — ✅
- Security hardening (confidence %, API keys/passwords/private IP detection, failsafe, safe mode) — ✅
- geek-dashboard/ — FastAPI + SQLite (tasks, requests_log, repos) — уже существует

### Фаза 4 — План (обсуждён 14 февраля):
- Структура отделов (разработка, бизнес, юридика, инвентаризация, R&D)
- Dashboard (Kanban) — веб-интерфейс (geek-dashboard уже есть как база)
- Маршрутизация задач по отделам
- Каждый отдел = набор правил роутинга + свои агенты

## Фаза 4: Отделы — прогресс

### Сделано:
- Sub-агент собрал Phase 4: departments (db.py, main.py, index.html обновлены)
- `departments` таблица + seed 6 отделов (Разработка, Бизнес, Юридика, Инвентаризация, R&D, CRM)
- `department_id` добавлен в tasks (ALTER migration)
- CRUD эндпоинты: GET/POST /api/departments, PUT /api/departments/{id}, GET /api/departments/{id}/tasks
- Frontend: карточки отделов, Kanban с drag-and-drop, фильтрация по отделам, table/kanban toggle
- venv создан в geek-dashboard/.venv/ (aiosqlite, fastapi, uvicorn)
- Сервер запущен на http://localhost:8080, API проверен — 6 departments loaded
- ⚠️ Ещё НЕ закоммичено в git

### Роутер + отделы интеграция ✅
- `qwen_router/departments.py` — keyword matching из dashboard DB, log_request_to_dashboard()
- router.py интегрирован: detect_department() в preprocess, department info в output JSON
- Department может override backend (lead_model) если нет manual override
- Тест: все 6 отделов матчатся корректно по keywords, "погода" → no match

### Dashboard v2 — полная переделка ✅
- Apple-стиль (SF Pro, frosted glass nav, blur modals, pill buttons)
- **System metrics** — CPU, RAM, Disk через subprocess (sysctl path: /usr/sbin/sysctl!), Ollama status
- **Git autodiscovery** — сканирует workspace (включая root .git), branch, last commit, dirty
- **Full task CRUD** — click to edit, delete with confirm, subtasks (parent_id), sort by priority
- **Tab navigation** — Обзор/Задачи/Запросы/Настройки с #hash routing
- **Cmd+K search** — tasks, departments, requests (SQLite LIKE case-sensitive для кириллицы — workaround с capitalize)
- **Department detail** — models, keywords, task counts, edit
- **Request analytics** — 14-day chart, model breakdown, PII audit columns
- DB schema: added department_id, pii_filtered, pii_categories to requests_log; path/branch/commit to repos

### Git (8 commits):
```
3a0eef5  Dashboard v2: system metrics, git autodiscovery, full CRUD, tabs, search, analytics
33a6890  Phase 4: departments + auto-assign + dashboard UI
11ce14e  security: hardening
693f1dd  Phase 3: skill + audit logging
41c6056  perf: postprocessor 4x faster
c973f02  Phase 2: router integrated
b606c35  chore: .gitignore
96e9275  Phase 1: baseline
```

### Баги пофикшенные:
- sysctl not found → /usr/sbin/sysctl (не в PATH в sandbox)
- repos scan не находил workspace root .git → добавлена проверка root
- SQLite LIKE case-sensitive для кириллицы → workaround с capitalize

### Сервер
- Запуск: `cd geek-dashboard/backend && ../.venv/bin/uvicorn main:app --port 8080`
- venv: geek-dashboard/.venv/ (aiosqlite, fastapi, uvicorn)

### Dashboard v3 — LLM Monitor + Task Steps + Mind Map ✅

**LLM Monitor:**
- Таблица `llm_providers` (Claude, ChatGPT, Gemini, Local/Ollama)
- Навбар: пиллы со статусом (green/yellow/red), клик → dropdown с бюджетами
- API проверяет env vars (ANTHROPIC_API_KEY и тд), Ollama /api/ps
- Сейчас: Claude/ChatGPT/Gemini = offline (ключи не в env), Local = online

**Task Steps (План выполнения):**
- Таблица `task_steps` (position, status, files, notes)
- Клик по задаче → slide-in панель справа (не маленький модал)
- Vertical stepper с прогресс-баром
- "Сгенерировать план" — шаблоны по отделам (dev: Анализ→Реализация→Тесты→Review→Деплой)
- Клик по шагу — cycling статуса (pending→in_progress→done→skipped)

**Interactive Mind Map:**
- Таблица `mind_map_nodes` (parent hierarchy, node_type, file_path, x/y positions)
- Pure Canvas API (NO libraries), DPI-aware (Retina)
- Bezier curves, drag nodes, zoom (scroll), pan (space+drag)
- Double-click: add/edit nodes. Right-click: context menu
- Auto-layout tree algorithm
- "Сгенерировать карту" — создаёт узлы из steps + сканирует workspace по keywords

### Git (9 commits):
```
51b3fcf  Dashboard v3: LLM monitor, task steps, mind map
3a0eef5  Dashboard v2: system metrics, autodiscovery, CRUD, tabs, search
33a6890  Phase 4: departments + auto-assign
11ce14e  security: hardening
693f1dd  Phase 3: skill + audit logging
41c6056  perf: postprocessor 4x faster
c973f02  Phase 2: router integrated
b606c35  chore: .gitignore
96e9275  Phase 1: baseline
```

### Следующее:
- Дима тестирует UI
- API ключи не в env — LLM monitor показывает offline для внешних провайдеров
- Фаза 5: RAG (ChromaDB), CRM, внешний диск
