# 2026-04-12

## Atlantic Construction CRM — Phase 1 Built

- Написал вручную (Claude Code не работал — не залогинен) полноценный Next.js app
- Стек: Next.js + TypeScript + TailwindCSS + Prisma + PostgreSQL
- Путь: `~/github/Atlantic-Construction-Remodeling-Group/src/`
- **21 таблица** — все 20 объектов из спецификации + ScopeRoom
- API routes: `/api/leads` (CRUD + `/api/leads/[id]`)
- Frontend: Dashboard, Lead List, Lead Create, Lead Detail
- Тёмная тема, sidebar навигация
- Запушено в GitHub (main)

## TODO
- PostgreSQL не установлен — нужен Docker Desktop или `brew install postgresql@17`
- Следующие страницы: Properties, Estimates, Proposals, Contracts, Projects, Permits, Vendors
- Claude Code: `/login` нужен для `claude` CLI

## Уроки
- Когда coding agent падает — **сразу сообщать Диме**, а не молча делать руками
- Дима явно сказал "нужно сообщать" — правило: всегда писать статус при запуске фоновых задач
