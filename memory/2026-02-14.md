# 2026-02-14

## Фаза 2: Qwen Router — собран и протестирован

- Пакет `qwen_router/` в workspace — полный пайплайн роутинга
- Файлы: config.py, pii.py, backends.py, router.py, cli.py, __init__.py, __main__.py, README.md
- venv в `qwen_router/.venv/` (httpx, anthropic)
- Запуск: `cd workspace && PYTHONPATH=. qwen_router/.venv/bin/python3 -m qwen_router -v "запрос"`
- Папка переименована из `qwen-router` → `qwen_router` (Python модуль)

### Что работает
- Классификация SIMPLE/COMPLEX/SENSITIVE через Qwen 14B — ✅
- PII regex (телефоны, email, паспорт, СНИЛС, ИНН, карты) — ✅
- PII семантический (Qwen убирает имена, города) — ✅
- Self-check (если PII утекает → перефильтровка) — ✅
- Перевод RU→EN — ✅
- Обратная адаптация ответа на RU — ✅
- Ручной override `/model <name>` — ✅
- Бэкенд Qwen (localhost) — ✅

### Рефакторинг v2: Препроцессор
- Роутер переделан в препроцессор — НЕ вызывает внешние API сам
- CLI: `pre` (classify+filter+translate → JSON) и `post` (adapt response → RU)
- OpenClaw получает JSON, сам вызывает Opus/Gemini/Codex (у него ключи)
- anthropic убран из requirements — не нужен
- backends.py теперь только Ollama (qwen_chat, deepseek_chat)
- Протестировано: "Дмитрий из Костромы" → "the user in a city in Russia" ✅

### Полный пайплайн протестирован end-to-end
- pre → Opus (через sessions_spawn) → post — работает
- Тест-запрос: "анализ рынка HR-tech SaaS" → Opus ответил → Qwen перевёл обратно
- route.sh — обёртка для CLI вызова (pre/post)
- Постпроцессор медленный: 80 сек на перевод длинного текста через Qwen 14B
  - Оптимизация: использовать 7B для переводов или пропускать если ответ уже на RU

### Git
- 3 коммита на master:
  - 96e9275 Phase 1 complete: workspace baseline
  - b606c35 chore: apply .gitignore
  - c973f02 Phase 2: Qwen Router integrated with OpenClaw

### Что не доделано
- Автоматический вызов роутера из основного flow (сейчас ручной через exec)
- Логирование что ушло наружу
- Оптимизация скорости постпроцессора
- Фаза 3: обёртки CLI + multi-pass + логирование

### Тайминги (на Mac mini M4)
- Классификация: 4-15 сек
- PII фильтр: ~6 сек
- Перевод: ~5 сек
- Self-check: ~10 сек
- Полный пайплайн без внешнего вызова: ~38 сек
- SIMPLE запрос (Qwen отвечает сам): ~26 сек
